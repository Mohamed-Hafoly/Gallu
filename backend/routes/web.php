<?php

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| This application is an API served to the Vue SPA in ../frontend, so there
| are no browser-facing routes here. The file itself stays: bootstrap/app.php
| registers it, and the `web` middleware group it brings is what Fortify's
| api-prefixed routes and Sanctum's /sanctum/csrf-cookie run on.
|
*/
